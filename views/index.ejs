<!DOCTYPE html>
<html lang="es">
  <head>
    <meta charset="utf-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <meta name="description" content="">
    <meta name="author" content="">

    <title>jointDeveloper</title>

    <!-- Bootstrap Core CSS -->
    <link href="css/bootstrap.min.css" rel="stylesheet">

    <!-- Custom CSS -->
    <link href="css/grayscale.css" rel="stylesheet">
    <!-- <link rel="shortcut icon" href="/img/favicon.ico"> -->

    <!-- Custom Fonts -->
    <link href="font-awesome-4.7.0/css/font-awesome.min.css" rel="stylesheet" type="text/css">
    <link href="http://fonts.googleapis.com/css?family=Lora:400,700,400italic,700italic" rel="stylesheet" type="text/css">
    <link href="http://fonts.googleapis.com/css?family=Montserrat:400,700" rel="stylesheet" type="text/css">
    <link href="https://fonts.googleapis.com/css?family=Linden+Hill:400i" rel="stylesheet">
    <link href="https://fonts.googleapis.com/css?family=Roboto:300" rel="stylesheet">
    <link href="https://fonts.googleapis.com/css?family=Oswald:300,400" rel="stylesheet">
  </head>
  <body id="page-top" data-spy="scroll" data-target=".navbar-fixed-top">
    <!-- Navigation -->
    <nav class="navbar navbar-custom navbar-fixed-top" role="navigation">
      <div class="container">
        <div class="navbar-header">
          <button type="button" class="navbar-toggle" data-toggle="collapse" data-target=".navbar-main-collapse">
            <i class="fa fa-bars"></i>
          </button>
          <a class="navbar-brand page-scroll" href="#page-top">jointDeveloper</a>
        </div>
        <!-- Collect the nav links, forms, and other content for toggling -->
        <div class="collapse navbar-collapse navbar-right navbar-main-collapse">
          <ul class="nav navbar-nav">
            <!-- Hidden li included to remove active class from about link when scrolled up past about section -->
            <li class="hidden">
              <a href="#page-top"></a>
            </li>
            <li>
              <a class="page-scroll" href="#about">Acerca de</a>
            </li>
            <li>
              <a class="page-scroll" href="#event">Eventos</a>
            </li>
            <li>
              <a class="page-scroll" href="#comunity">Comunidad</a>
            </li>
            <li>
              <a class="page-scroll" href="#blog">Social</a>
            </li>
            <li>
              <a class="page-scroll" href="#contact">Contacto</a>
            </li>
          </ul>
        </div>
        <!-- /.navbar-collapse -->
      </div>
      <!-- /.container -->
    </nav>

    <!-- Intro Header -->
    <header class="intro">
      <div class="intro-body">
        <div class="container">
          <div class="row">
            <div class="col-md-8 col-md-offset-2">
              <div><img src = "img/logo.png" class="img-responsive" style = "width: 100%; max-width: 350px; margin: 0 auto;"/></div>
              <h1 class="brand-heading roboto">
                Comunidad y Semillero de Investigación de la<br /> <b>Universidad Tecnológica de Pereira</b>
              </h1>
              <div class="row">
                  <% if(next[0].title !== ``) { %>
                    <div class="col-lg-12">
                      <h1 class="brand-heading oswald"><%= next[0].alert %></h1>
                      <div class="brand-heading oswald"><%= next[0].title %></div>
                      <h3><i class="fa fa-calendar" aria-hidden="true"></i> <%= next[0].date %> <i class="fa fa-clock-o" aria-hidden="true"></i> <%= next[0].hour %></h3>
                      <p class="oswald">
                        <b>Más información</b>
                      </p>
                    </div>
                  <% } else if(next[1].title !== ``) { %>
                    <div class="col-lg-12">
                      <h1 class="brand-heading oswald"><%= next[1].alert %></h1>
                      <div class="brand-heading oswald"><%= next[1].title %></div>
                      <h3><i class="fa fa-calendar" aria-hidden="true"></i> <%= next[1].date %> <i class="fa fa-clock-o" aria-hidden="true"></i> <%= next[1].hour %></h3>
                      <p class="oswald">
                        <b>Más información</b>
                      </p>
                    </div>
                  <% } %>
              </div>
              <a href="#about" class="btn btn-circle page-scroll">
                <i class="fa fa-angle-double-down animated"></i>
              </a>
            </div>
          </div>
        </div>
      </div>
    </header>

    <!-- About Section -->
    <div class="about">
      <section id="about" class="container content-section text-center">
      <div class="row">
        <div class="col-lg-8 col-lg-offset-2">
          <h1>Acerca de</h1>
          <p>Somos una comunidad que busca generar interés en la   mujer en áreas programación y tecnológia, por medio de espacios diversos que promueva su participación.
          </p>
          <p>Esta comunidad comparte conocimientos y recursos técnicos que ayudan a crear una red de intercambio con otras comunidades fortaleciendo nuestra formación y contribuyendo al desarrollo tecnológico.
          </p>
        </div>
        <div class="col-lg-8 col-lg-offset-2">
          <h1>Miembros</h1>
          <div class="carousel slide" id="myCarousel" data-ride="carousel">
            <% let amount = 4, keys = Object.keys(profiles); %>
            <div>
              <!-- Indicators -->
              <ol class="carousel-indicators">
                <% for (let i = 0; i < Math.ceil(keys.length / amount); i++) { %>
                  <% if (i == 0) { %>
                    <li data-target="#myCarousel" data-slide="<%= i %>" class="active"></li>
                  <% } else { %>
                    <li data-target="#myCarousel" data-slide="<%= i %>"></li>
                  <% } %>
                <% } %>
              </ol>
            </div>
            <!-- Wrapper for slides -->
            <div class="carousel-inner" role="listbox">
              <% for (let i = 0; i < keys.length; i++) { %>
                <% let cls = (i === 0) ? "item active" : "item" %>
                <% if (i % amount === 0) { %>
                  <div class="<%= cls %>">
                <% } %>
                <% if (i % 4 === 0) { %>
                  <div class="row">
                <% } %>

                <div class="col-lg-3 col-sm-6 col-xs-6">
                  <img src="<%= profiles[keys[i]].img %>" class="profileImg" alt="<%= profiles[keys[i]].name %>" />
                  <h4><%= profiles[keys[i]].name %></h4>
                </div>

                <% if (i % 4 === 3 || i === keys.length - 1) { %>
                  </div>
                <% } %>
                <% if (i % amount === amount - 1 || i === keys.length - 1) { %>
                  </div>
                <% } %>
              <% } %>
            </div>
          </div>
        </div>
        <div class="col-lg-8 col-lg-offset-2">
          <div class="col-lg-4 col-sm-6 col-xs-6 col-lg-offset-4 col-sm-offset-3 col-xs-offset-3">
            <img src="/img/profile/profile.svg" class="profileImg" alt="" />
          </div>
        </div>
        <div class="col-lg-8 col-lg-offset-2">
          <div class="col-lg-12 col-sm-12 col-xs-12">
          <h4>
            <b>Tú</b><br />
            También puedes ser parte de nuestra Comunidad<br />
            <a href="#"><b>Más información</b></a>
          </h4>
          </div>
        </div>
      </div>
    </section>
    </div>
    <!-- Register Section -->
    <div class="events">
      <section id="event" class="content-section text-center">
        <div class="container">
          <div class="col-lg-8 col-lg-offset-2">
            <h1>Eventos</h1>
            <p>Realizamos charlas mensuales sobre diferentes temas para incentivar y animar a más personas a que hagan parte de esta comunidad.
            </p>
            <div class="carousel slide" id="myCarousel2" data-ride="carousel">
              <% let j = 0, event = false, amountCh = 3; %>
              <% let offset = (next[0].title !== '') ? 1 : 0; %>
              <div>
                <!-- Indicators -->
                <ol class="carousel-indicators">
                  <% for (let i = 0; i < Math.ceil((charlas.length + offset) / amountCh); i++) { %>
                    <% if (i == 0) { %>
                      <li data-target="#myCarousel2" data-slide="<%= i %>" class="active"></li>
                    <% } else { %>
                      <li data-target="#myCarousel2" data-slide="<%= i %>"></li>
                    <% } %>
                  <% } %>
                </ol>
              </div>

               <!-- Wrapper for slides -->
               <% let nextCh = false, flag = false, ctn = 1; %>
               <div class="carousel-inner" role="listbox">
                <% if (next[0].title !== '') { %>
                  <% nextCh = true; %>
                  <div class="item active">
                    <div class="row">
                      <div class="col-lg-4 col-sm-12 event" style="padding: 15px;">
                        <h2><%= next[0].alert %></h2>
                        <p class="oswald"><%= next[0].title %></p>
                        <h4><i class="fa fa-calendar" aria-hidden="true"></i> <%= next[0].date %> <i class="fa fa-clock-o" aria-hidden="true"></i> <%= next[0].hour %></h4>
                        <h4><i class="fa fa-map-marker" aria-hidden="true"></i> <%= next[0].place %></h4>
                        <a class="btn">INSCRIPCIÓN</a>
                      </div>
                <% } %>
                <% for (let i = 0; i < charlas.length; i++) { %>
                  <% let clsCh = (i === 0 && !nextCh) ? "item active" : "item" %>
                  <% if (nextCh) { %>
                    <% if (flag) { %>
                      <% if (ctn % amountCh === 0) { %>
                        <div class="<%= clsCh %>">
                      <% } %>
                      <% if (ctn % 3 === 0) { %>
                        <div class="row">
                      <% } %>
                    <% } %>
                  <% } else { %>
                    <% if (i % amountCh === 0) { %>
                      <div class="<%= clsCh %>">
                    <% } %>
                    <% if (i % 3 === 0) { %>
                      <div class="row">
                    <% } %>
                  <% } %>

                  <div class="col-lg-4 col-sm-12 event"
                    <% if (charlas[i].image !== ``) { %>
                      style="background-image: url(/img/charla/<%= charlas[i].image %>/intro.jpg)"
                    <% } %>
                  >
                    <div class="overlay">
                      <% if (charlas[i].image !== ``) { %>
                        <img src="/img/charla/<%= charlas[i].image %>/logo.png" />
                      <% } %>
                      <h3><%= charlas[i].title %></h3>
                      <h4><i class="fa fa-calendar" aria-hidden="true"></i> <%= charlas[i].date %> <i class="fa fa-clock-o" aria-hidden="true"></i> <%= charlas[i].hour %></h4>
                      <h4><i class="fa fa-map-marker" aria-hidden="true"></i> <%= charlas[i].place %></h4>
                      <a class="btn">DETALLES</a>
                    </div>
                  </div>

                  <% if (nextCh) { %>
                    <% if (ctn % 3 === 2 || ctn === charlas.length) { %>
                      </div>
                    <% } %>
                    <% if (ctn % amountCh === amountCh - 1 || ctn === charlas.length) { %>
                      </div>
                    <% } %>
                  <% } else { %>
                    <% if (i % 3 === 2 || i === charlas.length - 1) { %>
                      </div>
                    <% } %>
                    <% if (i % amountCh === amountCh - 1 || i === charlas.length - 1) { %>
                      </div>
                    <% } %>
                  <% } %>
                  <% flag = true, ctn++; %>
                <% } %>
               </div>
            </div>
            <br />

            <p>Descubre los eventos que hemos realizado y anímate a ser parte de ellos.</p>
            <div class="carousel slide" id="myCarousel3" data-ride="carousel">
              <% let k = 0, ev = false, amountEv = 2  %>
              <% let offsetEv = (next[1].title !== '') ? 1 : 0; %>
              <div>
                <!-- Indicators -->
                <ol class="carousel-indicators">
                  <% for (let i = 0; i < Math.ceil((events.length + offsetEv) / amountEv); i++) { %>
                    <% if (i == 0) { %>
                      <li data-target="#myCarousel3" data-slide="<%= i %>" class="active"></li>
                    <% } else { %>
                      <li data-target="#myCarousel3" data-slide="<%= i %>"></li>
                    <% } %>
                  <% } %>
                </ol>
              </div>

              <!-- Wrapper for slides -->
              <% let nextEv = false, flag2 = false, ctn2 = 1; %>
              <div class="carousel-inner" role="listbox">
                <% if (next[1].title !== '') { %>
                  <% nextEv = true; %>
                  <div class="item active">
                    <div class="row">
                      <div class="col-lg-6 col-sm-12 event" style="padding: 10px;">
                        <h2><%= next[1].alert %></h2>
                        <p class="oswald"><%= next[1].title %></p>
                        <h4><i class="fa fa-calendar" aria-hidden="true"></i> <%= next[1].date %> <i class="fa fa-clock-o" aria-hidden="true"></i> <%= next[1].hour %></h4>
                        <h4><i class="fa fa-map-marker" aria-hidden="true"></i> <%= next[1].place %></h4>
                        <a class="btn">INSCRIPCIÓN</a>
                      </div>
                <% } %>
                <% for (let i = 0; i < events.length; i++) { %>
                  <% let clsEv = (i === 0 && !nextEv) ? "item active" : "item" %>
                  <% if (nextEv) { %>
                    <% if (flag2) { %>
                      <% if (ctn2 % amountEv === 0) { %>
                        <div class="<%= clsEv %>">
                      <% } %>
                      <% if (ctn2 % 3 === 0) { %>
                        <div class="row">
                      <% } %>
                    <% } %>
                  <% } else { %>
                    <% if (i % amountEv === 0) { %>
                      <div class="<%= clsEv %>">
                    <% } %>
                    <% if (i % 3 === 0) { %>
                      <div class="row">
                    <% } %>
                  <% } %>

                  <div class="col-lg-6 col-sm-12 event"
                    <% if (events[i].image !== ``) { %>
                      style="background-image: url(/img/event/<%= events[i].image %>/intro.jpg)"
                    <% } %>
                  >
                    <div class="overlay">
                      <% if (events[i].image !== ``) { %>
                        <img src="/img/event/<%= events[i].image %>/logo.png" />
                      <% } %>
                      <h3><%= events[i].title %></h3>
                      <h4><i class="fa fa-calendar" aria-hidden="true"></i> <%= events[i].date %> <i class="fa fa-clock-o" aria-hidden="true"></i> <%= events[i].hour %></h4>
                      <h4><i class="fa fa-map-marker" aria-hidden="true"></i> <%= events[i].place %></h4>
                      <a class="btn">DETALLES</a>
                    </div>
                  </div>

                  <% if (nextEv) { %>
                    <% if (ctn2 % 2 === 1 || ctn2 === events.length) { %>
                      </div>
                    <% } %>
                    <% if (ctn2 % amountEv === amountEv - 1 || ctn2 === events.length) { %>
                      </div>
                    <% } %>
                  <% } else { %>
                    <% if (i % 2 === 1 || i === events.length - 1) { %>
                      </div>
                    <% } %>
                    <% if (i % amountEv === amountEv - 1 || i === events.length - 1) { %>
                      </div>
                    <% } %>
                  <% } %>
                  <% flag2 = true, ctn2++; %>
                <% } %>
              </div>
            </div>
            <br />
          </div>
        </div>
      </section>
    </div>

    <!-- Contact Section -->
    <section id="comunity" class="container content-section text-center">
      <div class="row">
        <div class="col-lg-8 col-lg-offset-2">
          <h1>Comunidad</h1>
          <h3>Patrocinadores</h3>
          <% let u = 0; %>
          <% for (sponsor in sponsors) { %>
            <% u++; %>
            <% if (u % 4 === 1) { %>
              <div class="row">
            <% } %>
            <div class="col-lg-3 col-sm-12">
              <h1><%= u %></h1>
              <img src="/img/sponsor/<%= sponsor %>.png" class="img-responsive" />
              <br />
              <h4><%= sponsors[sponsor].name %></h4>
            </div>
            <% if (u % 4 === 0) { %>
              </div>
            <% } %>
          <% } %>
          <% if (sponsors.length % 4 !== 0) { %>
            </div>
          <% } %>
          <h3>Colaboradores</h3>
        </div>
      </div>
    </section>
    <div class="blog">
      <section id="blog" class="container content-section text-center">
        <div class="col-lg-8 col-lg-offset-2">
          <h1>Social</h1>
          <div class="row">
            <div class="col-lg-6">
              <a class="twitter-timeline" data-width="100%" data-height="auto" data-dnt="true" data-link-color="#E81C4F" href="https://twitter.com/jointdev">Tweets by jointdev</a> <script async src="//platform.twitter.com/widgets.js" charset="utf-8"></script>
            </div>
            <div class="col-lg-6">
              <h2>Blog</h2>
              hola
            </div>
          </div>
        </div>
      </section>
    </div>
    <!-- Footer -->
<% include footer %>
